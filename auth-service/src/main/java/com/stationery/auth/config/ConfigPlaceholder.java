package com.stationery.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ConfigPlaceholder {
    // This folder package is reserved for future custom configurations.
}
